{
  "nio/Buffer": {
    "version": "0.1.0",
    "description": "The Buffer block will collect all incoming signals and emit them every **interval**. If **interval_duration** is non-zero, then the signal emitted each **interval** will be all the signals collected over the last **interval_duration**, not just since the last emit.",
    "categories": [
      "Signal Flow"
    ],
    "properties": {
      "backup_interval": {
        "title": "Backup Interval",
        "type": "TimeDeltaType",
        "description": "",
        "default": {
          "seconds": 3600
        }
      },
      "group_by": {
        "title": "Group By",
        "type": "Type",
        "description": "Signal attribute to define groupings of incoming signals.",
        "default": null
      },
      "interval": {
        "title": "Buffer Interval",
        "type": "TimeDeltaType",
        "description": "Time interval at which signals are emitted.",
        "default": {
          "seconds": 1
        }
      },
      "interval_duration": {
        "title": "Interval Duration",
        "type": "TimeDeltaType",
        "description": "Each *interval* signals will emit going back this amount of time. If unspecifed or 0, then all input signals over the last **interval** will be emitted.",
        "default": null
      },
      "load_from_persistence": {
        "title": "Load from Persistence?",
        "type": "BoolType",
        "description": "If *True*, the block\u2019s state will be saved at a block stoppage and reloaded upon restart.",
        "default": true
      },
      "signal_start": {
        "title": "Start Interval On Signal?",
        "type": "BoolType",
        "description": "Start the interval when a signal is received",
        "default": false
      }
    },
    "inputs": {
      "default": {
        "description": ""
      }
    },
    "outputs": {
      "default": {
        "description": "Stored signals going back for the amount of time specified by the configured interval"
      }
    },
    "commands": {
      "emit": {
        "params": {
          "group": {
            "title": "group",
            "default": null,
            "allow_none": true
          }
        },
        "description": "Emit stored signals immediately"
      },
      "groups": {
        "params": {},
        "description": "View information on current groups"
      }
    }
  }
}